<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin/login</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

  </head>
  <body>
    <div id="admin-page-connexion">
      <div class="body-login">
          <img src="/images/logo.png" alt="" srcset="" />
        <form action="/admin/login" method="post"  >
          <div class="parent-input">
            <label for=""> <span>*</span> Email</label>
            <input type="text" placeholder="Email" name="usr_email"/>
          </div>
          <div class="parent-input">
            <label for=""> <span>*</span> Mot de passe</label>
            <input type="password" placeholder="Mot de passe" name="usr_pwd"/>
          </div>

          <div class="parent-button">
            <button type="submit">Connexion</button>
          </div>
        </form>
      </div>
      <div class="error"><% if (typeof errorMssg !== 'undefined') { %>
       <div class="alert alert-danger" role="alert">
          <%= (errorMssg) ? errorMssg : '' %>
        </div>
      <% } %>
    </div>
    </div>
  </body>
</html>
